---

# Role tests

- hosts: 'all'
  pre_tasks:
    - debug:
        var: role_name
  roles:
    - role: "{{ role_name }}"
  vars:
    role_name: "{{ playbook_dir | basename }}"
  vars_files:
    - './tests/test_vars.yml'
