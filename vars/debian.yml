---

# Debian specific vars

_elasticsearch_repository_version: "{{ (elasticsearch_version.split('.'))[0] }}"

_elasticsearch_packages:
  - 'elasticsearch'

_elasticsearch_service_name: 'elasticsearch'
